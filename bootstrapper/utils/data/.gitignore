# This directory is source code (bundled data files for the bootstrapper),
# not a Docker volume. Un-ignore everything here.
!*
# …except interpreter caches (the bare !* above re-included them and a
# .pyc once slipped into a commit via `git add -A`).
__pycache__/
*.pyc
