# GenAI Vanilla Stack - JupyterHub Python Dependencies
# AI/ML Libraries
ollama>=0.3.0
langchain>=0.3.0
langchain-community>=0.3.0
llama-index>=0.11.0
transformers>=4.40.0
sentence-transformers>=3.0.0

# Database Clients
weaviate-client>=4.7.0
neo4j>=5.20.0
psycopg2-binary>=2.9.9
redis>=5.0.0
supabase>=2.7.0
sqlalchemy>=2.0.30

# HTTP and API
httpx>=0.27.0
requests>=2.32.0

# Configuration and Utils
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Data Science (already in base image, but ensuring versions)
pandas>=2.2.0
numpy>=1.26.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0
networkx>=3.3

# Jupyter Extensions
ipywidgets>=8.1.0
jupyter-server-proxy>=4.1.0

# Deep Learning — PyTorch core
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# Deep Learning — higher-level frameworks
torchmetrics==1.4.2

# Graph Neural Networks (PyG) — wheels matched to torch 2.4 CPU build
--find-links https://data.pyg.org/whl/torch-2.4.0+cpu.html  # index uses minor version (covers torch 2.4.x)
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-cluster==1.6.3
torch-spline-conv==1.2.2
torch_geometric==2.6.1

# ML utilities
tqdm>=4.66.0
kaleido>=0.2.1

# Distributed compute
ray[client]>=2.55.1,<2.56

# ml-lab runtime support — see https://github.com/thekaveh/ml-lab
# Covers all 28 nnx-using Tier-A/B/C notebooks; the 29th (mnist-numpy from-scratch)
# additionally needs the ml-lab repo on disk for its sibling .py modules.
nnx-pytorch  # nnx core toolkit (source: https://github.com/thekaveh/NNx) — imported as `import nnx` by 28 of 29 ml-lab notebooks
python-louvain  # `community` module — community_detection-karate notebook
nltk  # SentimentIntensityAnalyzer — sentiment_classification-vader notebook (vader_lexicon downloaded in Dockerfile)
spacy  # tokenizer + lemmatizer — text_classification-agnews + sentiment_classification-vader (en_core_web_sm downloaded in Dockerfile)
torchao>=0.17  # transitive via nnx.quantize_int8 + nnx.qat_train_step_factory — quantization-mnist notebook
prettytable  # nnx Utils.print_table renderer — used by 14 of 29 notebooks
