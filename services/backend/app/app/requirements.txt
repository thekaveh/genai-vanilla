# API Framework
# fastapi 0.136 started pulling starlette 1.x, which introduced a new
# `_IncludedRouter` class for mounted sub-routers. prometheus-fastapi-
# instrumentator (both 7.x and 8.0.0) iterates the route list assuming
# every router has a `.path` attribute and trips an AttributeError on
# `_IncludedRouter` — see CI failure on PR #88 with fastapi 0.137.0 +
# starlette 1.3.1. Hold the floor below 0.136 until the upstream
# instrumentator ships a release that tolerates the new internal class.
fastapi>=0.110.0,<0.137
# uvicorn[standard] pulls watchfiles + uvloop + httptools + websockets.
# watchfiles is required for the Dockerfile's `--reload` CMD; the others
# are reasonable performance defaults the bare `uvicorn` distribution omits.
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
email-validator>=2.1.0
httpx>=0.27.0
python-multipart>=0.0.9
prometheus-fastapi-instrumentator>=7.0.0

# Database
sqlmodel>=0.0.16
sqlalchemy>=2.0.28
asyncpg>=0.31.0
storage3>=0.7.0

# Security
bcrypt>=4.1.2
python-jose>=3.3.0
passlib>=1.7.4

# AI/ML
ollama>=0.6.2
numpy>=1.26.0
pytz>=2024.1
apscheduler>=3.10.4
scikit-learn>=1.9.0
langchain>=0.1.14
langchain-core>=0.1.22
langchain-community>=0.0.24
langchain-openai>=1.3.3
# langchain-groq 0.3.x (latest stable) requires groq>=0.30.0,<1. The
# raw groq client was bumped to 1.4.0 by Dependabot without checking
# langchain-groq compatibility — pin groq into the langchain-groq
# accepted range so docker build resolves. Neither is imported by
# backend code today; LiteLLM handles Groq routing internally with
# its own client. Both kept as transitive ergonomics for callers
# that may import them directly later.
langchain-groq>=0.1.5
groq>=0.30.0,<1
langgraph>=0.0.15
transformers>=4.39.3

# Memory (LangMem-inspired persistent memory)
pgvector>=0.3.0
weaviate-client>=4.22.0

# Graph Database
neo4j>=5.18.0
langchain-neo4j>=0.1.0

# Redis
redis>=5.0.1
hiredis>=3.4.0

# Testing
pytest>=9.1.1
pytest-asyncio>=1.4.0

# n8n integration
requests>=2.31.0

# Manifest reading (comfyui selected-models.yaml)
pyyaml>=6.0

# Ray (distributed compute) — pin <2.56 so client/server stay in lockstep
# with the rayproject/ray:2.55.1 image; bump together when the image bumps.
ray[client]>=2.55.1,<2.56
