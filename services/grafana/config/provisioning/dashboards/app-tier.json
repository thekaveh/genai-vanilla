{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "title": "Weaviate — request rate (by API)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        { "expr": "sum(rate(requests_total{job=\"weaviate\"}[5m])) by (api)", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "title": "Weaviate — query p95 duration (ms)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(queries_durations_ms_bucket{job=\"weaviate\"}[5m])) by (le, query_type))", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "title": "MinIO — request rate (S3 API)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "sum(rate(minio_s3_requests_total[5m])) by (api)", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "title": "MinIO — cluster usage (bytes)",
      "type": "stat",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "minio_cluster_usage_total_bytes", "refId": "A" }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["weaviate", "minio", "apps"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "",
  "title": "App tier (Weaviate + MinIO)",
  "uid": "app-tier",
  "version": 1,
  "weekStart": ""
}
